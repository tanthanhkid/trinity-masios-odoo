services:
  masi-bot:
    build: .
    container_name: masi-bot
    restart: always
    env_file: .env
    healthcheck:
      test: ["CMD", "python", "-c", "print('ok')"]
      interval: 30s
      timeout: 5s
      retries: 3
